package com.keepsake.app.ui.theme

import androidx.compose.ui.graphics.Color

// Keepsake palette — grounded in the actual subject: aged recipe cards,
// kitchen index boxes, handwritten notes. Deliberately not the generic
// "warm cream + terracotta" AI-default — deeper parchment, a real
// paprika/wine accent instead of terracotta, and a herb-green secondary
// pulled from the kitchen, not the wellness-app palette.

val Ink = Color(0xFF2B231C)          // primary text — deep umber, like old ink
val InkSoft = Color(0xFF6B5D4F)      // secondary text
val Parchment = Color(0xFFEFE6D6)    // app background — aged paper, not bright cream
val Card = Color(0xFFFFFDF8)         // recipe card surface
val Line = Color(0xFFDCCFB3)         // hairline dividers, card edges

val Wine = Color(0xFF7A2E2A)         // primary accent — paprika/wine, not terracotta
val WineSoft = Color(0xFFF0DAD3)

val Herb = Color(0xFF6B7755)         // secondary accent — tags, categories
val HerbSoft = Color(0xFFE4E7D9)
